<Window x:Class="Wpf_L11_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_L11_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="600">

    <!--<Window.DataContext>
        <local:PersonContex/>
    </Window.DataContext>-->
    
    <Grid>
        <StackPanel>   
            <TextBlock Text="{Binding Path= Person.Name}"/>
            <TextBlock Text="{Binding Path= Person.Age}"/>

            <Button Content="Change" Command="{Binding ChangePerson}" Width="100"/>


            <!--<Button Content="Click Me" 
                    Command="{Binding MyCommand}"
                    CommandParameter="Button One"
                    FontSize="25"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"/>            
            <Button Content="Click Me 2" 
                    Command="{Binding MyCommand}"
                    CommandParameter="Button Two"
                    FontSize="25"
                    Margin="10"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"/>-->
            
        </StackPanel>
    </Grid>
    
</Window>
